window.addEventListener('DOMContentLoaded', (event) => {
    const audio = document.getElementById('background-audio');
    audio.muted = false;  // Desmutea el audio después de que la página haya cargado
});